// How many recent races the dashboard preview lists at most. The full list lives in
// AllRacesModal ("Show all races"). No height measurement is involved — on desktop the
// preview list scrolls inside the panel, on mobile the whole page scrolls.
export const TEACHER_RACES_PREVIEW_LIMIT = 4;
