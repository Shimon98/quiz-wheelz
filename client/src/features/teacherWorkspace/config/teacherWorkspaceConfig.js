/*
 * Shared knobs for the teacher workspace. Layout numbers live here so the
 * shell and its sections never hardcode magic values.
 */

// The dashboard home shows a short preview, not the full race history.
export const RACES_PREVIEW_LIMIT = 5;

export const WORKSPACE_NAVBAR_WIDTH = 280;

// Mantine breakpoint at which the navbar collapses into the burger drawer.
export const WORKSPACE_MOBILE_BREAKPOINT = "sm";

export const WORKSPACE_MOBILE_HEADER_HEIGHT = 56;
